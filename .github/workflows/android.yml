name: Android CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      working-directory: ./meowReader_client/Android

    - name: Build with Gradle
      run: ./gradlew assembleDebug
      working-directory: ./meowReader_client/Android

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: meowReader_client/Android/app/build/outputs/apk/debug/app-debug.apk
